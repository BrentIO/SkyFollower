services:

  receiver:
    image: ghcr.io/brentio/skyfollower-receiver:latest
    restart: unless-stopped
    volumes:
      - ./config/receiver/settings.json:/app/settings.json:ro
      - receiver-queue:/app/data
    networks:
      - skyfollower

volumes:
  receiver-queue:

networks:
  skyfollower:
    driver: bridge
