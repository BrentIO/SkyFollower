name: skyfollower-management-ui

# Runs on Host B (core) alongside docker-compose.core.yaml, not on
# the archive host -- management-ui's only dependency is Redis, which already
# lives on Host B. Kept as its own compose file (rather than folded into
# docker-compose.core.yaml) so it can move to a different host later
# without disturbing the rabbitmq/redis/runner stack.

services:

  management-ui:
    image: ghcr.io/brentio/skyfollower-management-ui:${SKYFOLLOWER_VERSION:-latest}
    restart: unless-stopped
    # nginx serves the built React frontend at / and proxies /api/* to
    # uvicorn, which listens on 127.0.0.1:8000 inside the container only.
    # 443 is HTTPS (self-signed by default, generated by scripts/install.sh
    # -- see management-ui/README.md's TLS section); 80 is redirect-only.
    ports:
      - "443:443"
      - "80:80"
    # Named one by one from this host's .env rather than `env_file: .env`,
    # so a container only ever holds the values it actually reads.
    environment:
      LOG_LEVEL: ${LOG_LEVEL:-info}
      REDIS_HOST: ${REDIS_HOST}
      REDIS_PORT: ${REDIS_PORT:-6379}
      REDIS_PASSWORD: ${REDIS_PASSWORD}
      S3_BUCKET: ${S3_BUCKET}
      AWS_DEFAULT_REGION: ${AWS_DEFAULT_REGION}
      AWS_ACCESS_KEY_ID: ${AWS_ACCESS_KEY_ID}
      AWS_SECRET_ACCESS_KEY: ${AWS_SECRET_ACCESS_KEY}
      ATHENA_WORKGROUP: ${ATHENA_WORKGROUP:-skyfollower}
      ATHENA_DATABASE: ${ATHENA_DATABASE:-skyfollower}
      ATHENA_TABLE: ${ATHENA_TABLE:-archive_flights}
    volumes:
      # config:rules/config:areas are the only Redis keys holding
      # user-authored state with no automatic regeneration path -- this
      # backend backs them up here on every write and restores from here
      # at startup if the Redis key is missing (see management-ui/README.md).
      # Bind mount, matching rabbitmq/redis on the core host -- the data
      # directory stays directly readable by the operator on the host.
      - ./data/management-ui:/app/data
      # Self-signed cert/key (or an operator's own, dropped in under the
      # same filenames), generated by scripts/install.sh -- see
      # management-ui/README.md's TLS section. Same host directory as the
      # rules/areas backup above, just a sibling subpath, so a fresh
      # install only ever needs the one ./data/management-ui tree.
      - ./data/management-ui/tls:/etc/nginx/tls:ro
    # No curl/wget in the image (only nginx is apt-get installed on top of
    # python:3.14-slim) -- use Python's built-in urllib instead of adding a
    # package just for this. Hits nginx's / (a real static file, index.html
    # via try_files fallback) to confirm nginx is actually serving traffic.
    # Certificate verification is deliberately disabled -- the cert is
    # self-signed by design (see management-ui/README.md's TLS section),
    # and this check only needs to know nginx answered on 443, not that a
    # public CA vouches for it.
    healthcheck:
      test: ["CMD", "python3", "-c", "import ssl, urllib.request; ctx = ssl.create_default_context(); ctx.check_hostname = False; ctx.verify_mode = ssl.CERT_NONE; urllib.request.urlopen('https://localhost/', timeout=3, context=ctx)"]
      interval: 30s
      timeout: 5s
      retries: 3
