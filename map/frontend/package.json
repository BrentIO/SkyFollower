{
  "name": "skyfollower-map-frontend",
  "private": true,
  "version": "0.0.0",
  "license": "GPL-3.0-or-later",
  "type": "module",
  "scripts": {
    "generate:shapes": "node scripts/generate-aircraft-shapes.mjs",
    "predev": "npm run generate:shapes",
    "dev": "vite",
    "prebuild": "npm run generate:shapes",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "pretypecheck": "npm run generate:shapes",
    "typecheck": "tsc -b",
    "pretest": "npm run generate:shapes",
    "test": "vitest run"
  },
  "dependencies": {
    "maplibre-gl": "^6.7.0",
    "react": "^19.3.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.3.3",
    "@types/geojson": "^7946.0.16",
    "@types/node": "^26.4.1",
    "@types/react": "^19.3.0",
    "@types/react-dom": "^19.2.7",
    "@vitejs/plugin-react": "^6.1.1",
    "svg-path-bbox": "^2.1.0",
    "tailwindcss": "^4.3.3",
    "typescript": "~7.0.2",
    "vite": "^8.2.2",
    "vitest": "^5.0.0"
  }
}
