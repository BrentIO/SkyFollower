# Specs

SkyFollower publishes machine-readable specs for its APIs:

- [AsyncAPI](/specs/asyncapi) — every MQTT topic published by the receiver, processor, archive processor, and rule notifications
- [OpenAPI](/specs/openapi) — the UI backend's REST API (not yet implemented)
