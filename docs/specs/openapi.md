# OpenAPI

::: tip Coming soon
The UI backend that would generate `specs/openapi.yaml` hasn't been built
yet (see the implementation phases in
[`CLAUDE.md`](https://github.com/BrentIO/SkyFollower/blob/main/CLAUDE.md)).
Once it exists, this page will render it interactively the same way the
[AsyncAPI page](/specs/asyncapi) renders `specs/asyncapi.yaml` today.
:::
