# OpenAPI

::: tip Coming soon
The UI backend that would generate `specs/openapi.yaml` hasn't been built
yet. Once it exists, this page will render it interactively the same way
the [AsyncAPI page](/specs/asyncapi) renders `specs/asyncapi.yaml` today.
:::
