{
  "name": "skyfollower-docs",
  "private": true,
  "type": "module",
  "scripts": {
    "docs:generate": "node scripts/generate-pages.mjs && node scripts/bundle-specs.mjs",
    "predocs:dev": "npm run docs:generate",
    "docs:dev": "vitepress dev",
    "predocs:build": "npm run docs:generate",
    "docs:build": "vitepress build",
    "docs:preview": "vitepress preview"
  },
  "devDependencies": {
    "@asyncapi/web-component": "^3.1.4",
    "vitepress": "^1.6.4"
  }
}
