{
  "_comment": "Template for the IAM identity archive-processor uses. __BUCKET_NAME__ is resolved by archive-processor at startup against its own s3.bucket config and written to {data_dir}/aws-setup/iam-policy.json -- paste that resolved file directly into the console's JSON policy editor (see docs/aws-setup.md).",
  "Version": "2012-10-17",
  "Statement": [
    {
      "Sid": "ConnectivityCheck",
      "Effect": "Allow",
      "Action": "s3:ListBucket",
      "Resource": "arn:aws:s3:::__BUCKET_NAME__"
    },
    {
      "Sid": "FlightAndIndexObjects",
      "Effect": "Allow",
      "Action": [
        "s3:GetObject",
        "s3:PutObject"
      ],
      "Resource": [
        "arn:aws:s3:::__BUCKET_NAME__/flights/*",
        "arn:aws:s3:::__BUCKET_NAME__/index/*"
      ]
    }
  ]
}
