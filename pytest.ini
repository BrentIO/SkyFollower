[pytest]
# importlib import mode lets multiple components each keep their own
# tests/__init__.py + tests/test_*.py layout without colliding on the same
# top-level "tests" module name when the whole repo is collected in one run
# (see #398).
addopts = --import-mode=importlib
norecursedirs = .venv venv node_modules .git
